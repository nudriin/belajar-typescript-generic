describe('Setup test', () => {
    it('should work as fine', () => {
        const sayHello = (name: string): void => {
            console.log(name);
        };

        sayHello('nurdin');
    });
});
